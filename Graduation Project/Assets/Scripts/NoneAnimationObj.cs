using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoneAnimationObj : ObjManager //애니메이션을 사용하지 않는 오브젝트 들
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
